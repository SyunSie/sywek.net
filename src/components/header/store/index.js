const state = {
  flashMsg: "",
};

const getters = {};

const actions = {};

const mutations = {};

const flashMsgStore = {
  flashMsgStore: {
    namespaced: true,
    state,
    getters,
    actions,
    mutations,
  },
};

export default flashMsgStore;
